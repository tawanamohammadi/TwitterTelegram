# 🌐 UNFEM Twitter to Telegram Service

سرویس خودکار انتقال توییت‌های UN Women از توییتر به تلگرام

## 📋 درباره پروژه

این پروژه یک سرویس کامل وب است که به صورت خودکار توییت‌های جدید از اکانت توییتر UN Women (یا هر اکانت دیگری) را دریافت کرده و به کانال یا گروه تلگرام شما ارسال می‌کند. این سرویس شامل یک داشبورد وب برای مدیریت، پیکربندی و نظارت بر عملکرد سیستم است.

### ویژگی‌های اصلی

✅ **دریافت خودکار توییت‌ها**: با استفاده از Twitter API v2 برای دریافت آخرین توییت‌ها
✅ **ارسال به تلگرام**: ارسال خودکار توییت‌ها به کانال یا گروه تلگرام
✅ **داشبورد مدیریتی**: رابط کاربری زیبا و کاربرپسند برای مدیریت
✅ **زمان‌بندی قابل تنظیم**: تعیین فاصله زمانی بررسی توییت‌ها (به دقیقه)
✅ **قالب‌بندی پیام**: امکان سفارشی‌سازی قالب پیام‌های ارسالی
✅ **پشتیبانی از تصاویر**: ارسال تصاویر همراه با توییت‌ها
✅ **گزارش‌گیری**: نمایش آمار و لاگ‌های فعالیت
✅ **کنترل دستی**: امکان بررسی فوری و ارسال پیام تست
✅ **مدیریت وضعیت**: روشن/خاموش کردن سرویس از داشبورد

## 🛠 تکنولوژی‌های استفاده شده

### Backend
- **Node.js & Express**: سرور REST API
- **TypeScript**: برای type safety و توسعه بهتر
- **node-cron**: برای زمان‌بندی وظایف
- **Twitter API v2**: دریافت توییت‌ها
- **Telegram Bot API**: ارسال پیام‌ها
- **Drizzle ORM**: مدیریت دیتابیس
- **Zod**: اعتبارسنجی داده‌ها

### Frontend
- **React 18**: کتابخانه UI
- **Vite**: ابزار بیلد سریع
- **Wouter**: مسیریابی سبک
- **TanStack Query (React Query)**: مدیریت state و cache
- **React Hook Form**: مدیریت فرم‌ها
- **shadcn/ui**: کامپوننت‌های UI
- **Tailwind CSS**: استایل‌دهی
- **Lucide React**: آیکون‌ها

### Database
- **PostgreSQL**: ذخیره‌سازی داده (پشتیبانی از Neon Database)
- **In-Memory Storage**: گزینه جایگزین برای محیط توسعه

## 📦 نصب و راه‌اندازی

### پیش‌نیازها

- Node.js (نسخه 20 یا بالاتر)
- npm یا yarn
- Twitter Developer Account (برای API keys)
- Telegram Bot Token
- دیتابیس PostgreSQL (اختیاری)

### مراحل نصب

1. **کلون کردن پروژه**
```bash
git clone <repository-url>
cd unfem-twitter-telegram
```

2. **نصب وابستگی‌ها**
```bash
npm install
```

3. **تنظیم متغیرهای محیطی**

فایل `.env` در ریشه پروژه ایجاد کنید:

```env
# Twitter API Credentials
TWITTER_API_KEY=your_twitter_api_key
TWITTER_API_SECRET=your_twitter_api_secret
TWITTER_BEARER_TOKEN=your_twitter_bearer_token

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_CHANNEL=@your_channel_or_chat_id

# Database (اختیاری - در صورت استفاده از PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database
```

4. **راه‌اندازی دیتابیس** (در صورت استفاده از PostgreSQL)
```bash
npm run db:push
```

5. **اجرای پروژه در محیط توسعه**
```bash
npm run dev
```

پروژه روی `http://localhost:5000` در دسترس خواهد بود.

6. **بیلد برای محیط تولید**
```bash
npm run build
npm start
```

## ⚙️ پیکربندی

### دریافت Twitter API Keys

1. به [Twitter Developer Portal](https://developer.twitter.com/) بروید
2. یک پروژه و اپلیکیشن جدید بسازید
3. کلیدهای API را از بخش "Keys and tokens" کپی کنید:
   - API Key
   - API Secret Key
   - Bearer Token

### ساخت Telegram Bot

1. در تلگرام با [@BotFather](https://t.me/botfather) چت کنید
2. دستور `/newbot` را ارسال کنید
3. نام و username برای ربات انتخاب کنید
4. توکن ربات را کپی کنید
5. ربات را به کانال یا گروه خود اضافه کنید و ادمین کنید

### تنظیمات از داشبورد

بعد از اجرای پروژه، به صفحه Configuration بروید و موارد زیر را تنظیم کنید:

- **Twitter Account**: نام کاربری اکانت توییتر (بدون @)
- **Check Interval**: فاصله زمانی بررسی توییت‌های جدید (به دقیقه)
- **Twitter API Credentials**: کلیدهای API توییتر
- **Telegram Settings**: توکن ربات و آیدی کانال/گروه
- **Message Template**: قالب پیام‌های ارسالی (از placeholder‌های `{tweet_text}` و `{tweet_url}` استفاده کنید)
- **Include Images**: فعال/غیرفعال کردن ارسال تصاویر

## 📱 استفاده

### داشبورد

در صفحه اصلی (Dashboard) می‌توانید:
- وضعیت سرویس را مشاهده کنید (فعال/غیرفعال)
- آمار کلی را ببینید (تعداد توییت‌های ارسال شده، زمان آخرین بررسی، uptime)
- لاگ‌های فعالیت را مشاهده کنید
- سرویس را روشن یا خاموش کنید
- بررسی فوری برای توییت‌های جدید انجام دهید
- پیام تستی به تلگرام ارسال کنید

### صفحات

- **Dashboard** (`/`): نمای کلی و کنترل سرویس
- **Configuration** (`/config`): تنظیمات و پیکربندی
- **Logs** (`/logs`): مشاهده تاریخچه کامل لاگ‌ها
- **Help** (`/help`): راهنما و پاسخ به سوالات متداول

## 🔌 API Endpoints

### Configuration

- `GET /api/config` - دریافت تنظیمات فعلی
- `PATCH /api/config` - به‌روزرسانی تنظیمات

### Stats

- `GET /api/stats` - دریافت آمار سرویس

### Logs

- `GET /api/logs` - دریافت لیست لاگ‌ها

### Actions

- `POST /api/check-now` - بررسی فوری برای توییت‌های جدید
- `POST /api/test-telegram` - ارسال پیام تستی به تلگرام
- `POST /api/reset` - ریست کردن آمار سرویس

## 📁 ساختار پروژه

```
├── client/                    # Frontend React Application
│   ├── src/
│   │   ├── components/       # کامپوننت‌های React
│   │   │   ├── ui/          # کامپوننت‌های shadcn/ui
│   │   │   ├── ConfigurationForm.tsx
│   │   │   ├── Layout.tsx
│   │   │   ├── LogsTimeline.tsx
│   │   │   ├── ManualActions.tsx
│   │   │   ├── ServiceToggle.tsx
│   │   │   ├── Sidebar.tsx
│   │   │   └── StatusCard.tsx
│   │   ├── pages/           # صفحات اپلیکیشن
│   │   │   ├── Dashboard.tsx
│   │   │   ├── Configuration.tsx
│   │   │   ├── Logs.tsx
│   │   │   └── Help.tsx
│   │   ├── lib/             # توابع کمکی
│   │   ├── hooks/           # React hooks
│   │   ├── App.tsx          # کامپوننت اصلی
│   │   └── main.tsx         # نقطه ورود
│   └── index.html
│
├── server/                   # Backend Express Server
│   ├── index.ts            # نقطه ورود سرور
│   ├── routes.ts           # API routes
│   ├── scheduler.ts        # منطق زمان‌بندی و پردازش توییت‌ها
│   ├── storage.ts          # لایه دسترسی به دیتابیس
│   ├── telegram.ts         # کلاینت Telegram API
│   ├── twitter.ts          # کلاینت Twitter API
│   └── vite.ts             # تنظیمات Vite
│
├── shared/                  # کد مشترک بین Frontend و Backend
│   └── schema.ts           # مدل‌های دیتابیس و اسکیماهای Zod
│
├── package.json            # وابستگی‌ها و اسکریپت‌ها
├── tsconfig.json           # تنظیمات TypeScript
├── vite.config.ts          # تنظیمات Vite
├── tailwind.config.ts      # تنظیمات Tailwind CSS
├── drizzle.config.ts       # تنظیمات Drizzle ORM
└── README.md               # این فایل!
```

## 🔄 نحوه کار سرویس

1. **زمان‌بندی**: سرویس با استفاده از `node-cron` در بازه‌های زمانی مشخص (قابل تنظیم) اجرا می‌شود

2. **دریافت توییت‌ها**: از Twitter API v2 برای دریافت آخرین توییت‌های اکانت مورد نظر استفاده می‌شود

3. **بررسی تکراری**: سیستم بررسی می‌کند که آیا توییت قبلاً پردازش شده یا نه

4. **قالب‌بندی**: پیام با استفاده از template مشخص شده فرمت می‌شود

5. **ارسال به تلگرام**: پیام و در صورت نیاز تصاویر به کانال تلگرام ارسال می‌شوند

6. **ثبت لاگ**: تمام فعالیت‌ها و خطاها در سیستم ثبت می‌شوند

7. **به‌روزرسانی آمار**: تعداد توییت‌های ارسال شده و سایر آمارها به‌روز می‌شوند

## 🗄️ مدل دیتابیس

### Tables

#### `config`
تنظیمات سیستم:
- `twitterAccount`: نام کاربری توییتر
- `checkInterval`: فاصله زمانی بررسی (دقیقه)
- `twitterApiKey`, `twitterApiSecret`, `twitterBearerToken`: اعتبارنامه‌های Twitter API
- `telegramToken`: توکن ربات تلگرام
- `telegramChannel`: آیدی کانال/گروه
- `messageTemplate`: قالب پیام
- `includeImages`: ارسال تصاویر (boolean)
- `serviceActive`: وضعیت سرویس (boolean)
- `lastCheck`: آخرین زمان بررسی

#### `tweets`
توییت‌های پردازش شده:
- `tweetId`: شناسه یکتا توییت
- `text`: متن توییت
- `url`: لینک توییت
- `processedAt`: زمان پردازش

#### `logs`
لاگ‌های سیستم:
- `type`: نوع لاگ (info, success, error, warning)
- `message`: پیام
- `details`: جزئیات اضافی
- `timestamp`: زمان ثبت

#### `stats`
آمار سرویس:
- `tweetsForwarded`: تعداد کل توییت‌های ارسال شده
- `serviceStartTime`: زمان شروع سرویس
- `lastReset`: آخرین ریست

## 🎨 سفارشی‌سازی

### تغییر قالب پیام

از صفحه Configuration می‌توانید قالب پیام را تغییر دهید. متغیرهای قابل استفاده:

- `{tweet_text}`: متن توییت
- `{tweet_url}`: لینک توییت

مثال:
```
📢 *توییت جدید از UN Women*

{tweet_text}

🔗 [مشاهده در توییتر]({tweet_url})
```

### تغییر تم رنگی

فایل `theme.json` را ویرایش کنید:
```json
{
  "primary": "#رنگ_اصلی",
  "variant": "professional",
  "appearance": "light",
  "radius": 0.5
}
```

## 🚀 اسکریپت‌های npm

```bash
npm run dev        # اجرا در محیط توسعه
npm run build      # بیلد برای production
npm start          # اجرای نسخه production
npm run check      # بررسی خطاهای TypeScript
npm run db:push    # اعمال تغییرات schema به دیتابیس
```

## 🐛 رفع مشکلات

### سرویس توییت‌ها را دریافت نمی‌کند

- بررسی کنید که Twitter API credentials صحیح باشند
- محدودیت‌های Twitter API را چک کنید (rate limits)
- لاگ‌ها را از صفحه Logs بررسی کنید

### پیام‌ها به تلگرام ارسال نمی‌شوند

- توکن ربات تلگرام را بررسی کنید
- مطمئن شوید که ربات به کانال اضافه شده و Admin است
- Chat ID یا username کانال را درست وارد کرده باشید (برای کانال عمومی: `@channel_username`)

### خطاهای دیتابیس

- مطمئن شوید که `DATABASE_URL` در `.env` درست تنظیم شده
- دستور `npm run db:push` را اجرا کنید
- در صورت استفاده از in-memory storage، نیازی به دیتابیس نیست

### سرویس خودکار متوقف می‌شود

- لاگ‌های سرور را بررسی کنید
- از بخش Manual Actions، دکمه "Check Now" را امتحان کنید
- سرویس را از داشبورد خاموش و روشن کنید

## 📝 توسعه

### افزودن ویژگی جدید

1. مدل داده را در `shared/schema.ts` اضافه کنید
2. interface storage را در `server/storage.ts` به‌روز کنید
3. route جدید را در `server/routes.ts` بسازید
4. کامپوننت و صفحه مورد نیاز را در `client/src` اضافه کنید

### کار با Database

این پروژه از Drizzle ORM استفاده می‌کند. برای تغییر schema:

1. فایل `shared/schema.ts` را ویرایش کنید
2. دستور `npm run db:push` را اجرا کنید

## 🔒 امنیت

- تمام API credentials باید در متغیرهای محیطی ذخیره شوند
- هرگز فایل `.env` را commit نکنید
- از HTTPS برای production استفاده کنید
- به‌طور منظم توکن‌ها و کلیدها را بازنشانی کنید

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## 🤝 مشارکت

برای مشارکت در پروژه:

1. Fork کنید
2. برنچ جدید بسازید (`git checkout -b feature/amazing-feature`)
3. تغییرات را commit کنید (`git commit -m 'Add amazing feature'`)
4. Push کنید (`git push origin feature/amazing-feature`)
5. Pull Request باز کنید

## 📞 پشتیبانی

در صورت بروز مشکل یا سوال:

1. صفحه Help در داشبورد را مطالعه کنید
2. لاگ‌ها را از صفحه Logs بررسی کنید
3. Issue در GitHub باز کنید

## 🎯 نقشه راه

- [ ] پشتیبانی از چند اکانت توییتر
- [ ] فیلترهای پیشرفته برای توییت‌ها
- [ ] گزارش‌های دوره‌ای
- [ ] احراز هویت و مدیریت کاربران
- [ ] نوتیفیکیشن‌های داخلی
- [ ] پشتیبانی از چند کانال تلگرام
- [ ] آمار پیشرفته و نمودارها

---

**ساخته شده با ❤️ برای UN Women Community**
